package it.scalachess.server.test

import org.scalatest.{ FlatSpec, Inspectors, Matchers, OptionValues }

class ServerSpec extends FlatSpec with Matchers with OptionValues with Inspectors {

//  val p1: ActorRef[ClientMessage] = ActorSystem(ServerProxy(), "testClient")

  "LobbyMap" should "be updated" in {
//    val p1 = ActorRef()
//    val lobbyMap: LobbyMap = Map("A" -> Lobby("A", players = ()))
//    val newlobbyMap =
  }
}
